export type SampleAT = {
  a: string;
  b: number;
};

export type SampleBT = [a: string, b: number];

export type PostT = {
  author: string;
  body: string;
};

export type ModalT = {
  children: React.ReactNode;
};
